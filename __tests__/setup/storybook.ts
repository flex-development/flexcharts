/**
 * @file Test Setup - Storybook
 * @module tests/setup/storybook
 */

import { setProjectAnnotations } from '@storybook/react'
import * as preview from 'storybook/preview'

setProjectAnnotations(preview)

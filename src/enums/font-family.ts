/**
 * @file Enums - FontFamily
 * @module enums/FontFamily
 */

/**
 * `theme.fonts` map.
 *
 * @enum {string}
 */
const FontFamily = Object.freeze({
  basier: 'basier-circle'
} as const)

export default FontFamily

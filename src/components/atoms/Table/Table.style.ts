/**
 * @file Table - style
 * @module components/atoms/Table/style
 */

import type { CSSFragment, ThemedProps } from 'src/types'
import { css } from 'styled-components/macro'

/**
 * Applies component styles.
 *
 * @param {ThemedProps} p - Component props
 * @return {CSSFragment} Component styles
 */
const style = (p: ThemedProps): CSSFragment => css``

export default style
